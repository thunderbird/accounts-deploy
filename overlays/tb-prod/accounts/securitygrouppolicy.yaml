# Assigns a dedicated pod security group to the Accounts web + celery pods so they
# can reach the Accounts RDS Postgres, ElastiCache Redis, and the Stalwart
# management API. Under POD_SECURITY_GROUP_ENFORCING_MODE=strict the listed SGs
# REPLACE the node SG, so the EKS control-plane cluster SG must also be listed:
#   - sg-08711bcc50c7b1e18 : tb-prod EKS control-plane cluster SG (pod<->control-plane)
#   - REPLACE_TB_PROD_ACCOUNTS_POD_SG : mzla-tb-prod-accounts-pod, from the
#       mzla-tb-prod pulumi output (thunderbird/platform-infrastructure#596)
apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: accounts
  namespace: accounts
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: accounts
  securityGroups:
    groupIds:
      - sg-08711bcc50c7b1e18
      - REPLACE_TB_PROD_ACCOUNTS_POD_SG
